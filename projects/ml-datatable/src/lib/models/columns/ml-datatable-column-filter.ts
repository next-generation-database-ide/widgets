import {MLDatatableColumnFilterMode} from './ml-datatable-column-filter-mode';

export class MLDatatableColumnFilter {
  dataUrl?: string;
  filterMode: MLDatatableColumnFilterMode;
  regexPattern?: string;
}
